import { StyleSheet } from 'react-native';
import * as constSts from '@constants/style';

export default ldScreenSts = StyleSheet.create({
    container:{
        position:"absolute",
        width:"100%",
        height:"100%",
        left:0,
        top:0,
        backgroundColor:"rgba(0, 0, 0, 0.6)",
        alignItems: 'center',
        justifyContent:"center"
    }
});